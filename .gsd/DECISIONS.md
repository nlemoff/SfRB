# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | distribution | Local CLI + Browser UI | Node.js + Vite | Avoids native app bloat, leverages user's browser, enables CLI parity. | No |
| D002 | M001 | data | BYOK AI Integration | OpenAI/Anthropic keys | Privacy, user ownership, and zero operational cost. | Yes |
| D003 | M001 | arch | Configurable Physics | Doc-style vs Design-style | Adapts to different user mental models for layout. | Yes |
| D004 | M001 | arch | AI Layout Consultant | Fixed boxes + Previews | Solves "broken design" of spatial tools without taking away control. | No |
| D005 | M001 | data | Document Format | JSON-based Semantic + Spatial | Enables CLI/GUI parity and agent-friendliness. | No |
| D006 | M001 | cli | CLI stack for init/config | Commander + Enquirer + Zod + project-local config store | Gives S01 a conventional CLI surface, masked prompts, and a stable validated config boundary for later slices. | Yes |
| D007 | M001 | config | S01 config contract | `sfrb.config.json` stores provider + provider-specific env-var name + workspace physics, with `document` as the default physics mode | Keeps secrets out of the config file itself while giving later CLI/web slices one validated workspace-local contract to consume. | Yes |
| D008 | M001 | cli | Init automation path | Drive automated init coverage through `SFRB_INIT_TEST_INPUT` so tests and smoke runs execute the real command flow without faking prompt internals | Preserves a real Enquirer wizard for users while giving agents a stable non-TTY harness that still exercises command persistence, redaction, and gitignore behavior. | Yes |
| D009 | M001 | data | S02 document schema source of truth | Define the SfRB document contract as strict Zod schemas and emit the checked-in `schema.json` artifact from that same source | Keeps runtime parsing, TypeScript types, and published JSON Schema aligned for later CLI/browser/agent consumers. | Yes |
| D010 | M001 | data | S02 workspace document file contract | Reserve `resume.sfrb.json` as the default workspace document path for the canonical local document model | Gives S03+ one explicit file target to watch/open while preserving JSON tooling ergonomics. | Yes |
| D011 | M001 | validation | S02 workspace physics validation | Use `sfrb.config.json` as the only physics source of truth; `document` mode forbids fixed `layout.frames`, while `design` mode requires every semantic block to have a linked frame | Keeps physics state out of the document file itself while making document-vs-design behavior observably different at the workspace boundary. | Yes |
| D012 | M001 | arch | S03 local web bridge runtime boundary | Keep the shipped CLI on its current CommonJS build, but launch a separate ESM Vite bridge runtime that loads workspace state via `readWorkspaceDocument()` and pushes file changes over Vite custom events | Avoids the Vite ESM/CommonJS interop trap, reuses Vite’s existing transport, and preserves one authoritative document boundary for later editor slices. | Yes |
| D013 | M001 | observability | S03 bridge process contract | Use parent/child IPC for `bridge-ready` / startup-failure reporting, while browser-facing live updates continue over Vite custom events (`sfrb:bridge-update`, `sfrb:bridge-error`) and the `/__sfrb/bootstrap` payload | Gives the CLI a deterministic readiness/failure handshake for tests and automation without adding a second browser transport or duplicating bridge state shapes. | Yes |
| D014 | M001 | arch | S03 bridge HTML/bootstrap routing | Keep the bridge on Vite `appType: 'custom'`, serve `/` through `transformIndexHtml()`, and disable automatic dep discovery for the custom bridge server | Prevents Vite’s SPA fallback from swallowing `/__sfrb/bootstrap` and avoids shutdown-time dep-scan noise in CLI-driven bridge tests while preserving the real HMR transport for the browser shell. | Yes |
| D015 | M001 | web | S03 browser shell reconciliation | Treat Vite custom events only as refetch triggers and always reconcile browser state from `/__sfrb/bootstrap`; keep the shell dependency-light so it runs cleanly inside the custom bridge server | Preserves one canonical document shape, keeps invalid-state handling centralized in the bridge payload, and avoids extra client-runtime coupling at the CLI↔browser boundary. | Yes |
| D016 | M001 | web | S04 editor architecture | Build the editor as a DOM-first surface over the canonical bridge, with server-validated browser writes and mode-specific rendering for document vs design physics | Avoids canvas-text complexity, preserves S03’s single-source-of-truth contract, and gives S04/S05 one shared editing foundation that can enforce workspace physics at the bridge boundary. | Yes |
| D017 | M001 | bridge | S04 editor write/reconciliation contract | Keep `/__sfrb/bootstrap` as the only canonical document inspection surface, add a separate `/__sfrb/editor` mutation route that validates schema + physics before persistence, and let browser save-state hooks mirror mutation responses instead of storing a second document copy | Preserves watcher-driven refetch reconciliation, makes write failures path-aware and inspectable, and gives later UI work a stable idle/saving/error source without forking the document model. | Yes |
| D018 | M001 | web | T02 document-mode canvas reconciliation | Reconcile document-mode canvas updates by stable semantic structure and targeted DOM patching, instead of rebuilding the block tree on every ready-payload refresh | Preserves the active textarea/caret through save-triggered bridge refetches, keeps selection/save observability stable, and leaves room for T03 to layer design-mode behavior onto the same engine without reintroducing destructive rerenders. | Yes |
| D019 | M001 | web | T03 design-mode interaction split | In design physics, render canonical frame geometry with a dedicated drag handle for box movement and keep text editing on double-click inside the linked frame rather than starting edit on every click | Prevents drag-vs-edit input conflicts, keeps frame selection diagnostics stable, and preserves block↔frame linkage while sharing the same editor engine and bridge write path as document mode. | Yes |
| D020 | M001 | arch | S05 consultant runtime split | Detect overflow and render ghost previews in the browser, but request structured resize proposals only through a bridge-backed consultant route; keep preview geometry separate from canonical engine frame overrides until explicit accept. | Preserves the BYOK secret boundary, lets the browser measure real rendered overflow cheaply, and keeps rejectable preview state from contaminating the canonical working document path. | Yes |
| D021 | M001 | bridge | S05 consultant provider boundary | Let the bridge resolve `sfrb.config.json` provider metadata plus env-backed secrets, call providers through a tiny structured-output adapter, and return only validated frame-resize proposals or sanitized categorized failures. | Keeps secrets and raw provider payloads out of the browser while giving later UI/runtime tests stable consultant codes (`configuration_missing`, `provider_unavailable`, `malformed_provider_output`, etc.) to inspect. | Yes |
| D022 | M001 | web | T02 overflow/preview ownership split | Measure overflow inside Canvas against the rendered frame body, but keep consultant request/apply state in App and never route ghost preview geometry through engine `frameBoxOverrides` before accept. | Preserves a DOM-truth overflow signal, keeps reject non-destructive, and makes stale-preview invalidation depend on canonical payload identity instead of editor-local overrides. | Yes |
| D023 | M002 | product | Primary user and milestone emphasis | Optimize M002 for a non-technical user who should be able to make a good resume fast, and explicitly de-emphasize AI while the editor engine and product feel take priority. | Keeps the next milestone focused on real product usefulness instead of drifting back into provider/agent work before the editor experience is trustworthy. | Yes |
| D024 | M002 | arch | Guided editor model | Treat text mode, tile mode, and freeform mode as guided editing lenses over one canonical resume engine rather than separate editors. | Preserves one coherent product and document model while allowing different interaction styles for different users and tasks. | Yes |
| D025 | M002 | cli | CLI parity interpretation | Define CLI parity as structured action/model parity: every meaningful editor action must be representable and invokable from the CLI, even if the browser remains the primary human UX. | Keeps the system scriptable and agent-ready without forcing the CLI to mimic the browser as the preferred everyday interface for non-technical users. | Yes |
| D026 | M002 | scope | Export sequencing | Stage reliable PDF export as M003 so M002 can focus on editor-engine coherence, guidance, and product trust first. | Export matters, but it depends on whichever editing/reconciliation behaviors M002 proves trustworthy; sequencing it later avoids planning against a moving engine target. | Yes |
| D027 | M002 | first-run | Starter-workspace onboarding boundary | Materialize template/blank starts as real canonical workspaces during init, carry starter identity in canonical state, and keep `sfrb open` focused on serving that state rather than inventing a separate onboarding runtime. | Preserves the existing bridge/bootstrap seam, makes non-AI first-run honest, and gives later slices durable starter identifiers without browser-only state. | Yes |
| D028 | M002 | data | Starter metadata placement | Store shipped starter identity as optional `document.metadata.starter` and generate starter documents through one production factory shared by tests/runtime. | Keeps starter provenance inspectable in `resume.sfrb.json` without breaking non-starter documents, and avoids duplicate hand-written starter shapes drifting across test/runtime paths. | Yes |
| D029 | M002 | cli | First-run workspace init contract | Allow `sfrb.config.json` to omit `ai`, make `sfrb init` require explicit starter + physics selection, and report AI state as `skipped` vs `configured` while always materializing `resume.sfrb.json`. | Makes editor-only workspaces truthful on day one, preserves the configured-provider path without leaking secrets, and leaves later bridge/browser work with one inspectable workspace state instead of an AI-first stub. | Yes |
| D030 | M002 | bridge/web | First-run guidance state source | Expose starter identity plus a small AI availability envelope on `/__sfrb/bootstrap`, and drive the browser shell + consultant unavailable state from that canonical payload instead of client-local onboarding heuristics. | Keeps first-run copy truthful to the real workspace, makes AI-skipped/degraded states inspectable in one place, and preserves `/__sfrb/bootstrap` as the single browser truth during save/refetch reconciliation. | Yes |
| D031 | M003 planning | planning | How contributor-facing planning should relate to the GSD milestone docs | Keep M001 and M002 as fixed historical milestones, and add a streamlined public build plan plus contributor-track annotations for M003+ so open-source contributors can pick up work without learning the full GSD document set first. | The first two milestones already describe shipped foundation work and should not be re-litigated. A contributor-facing roadmap layered on top of the GSD source docs makes future slices easier to join while preserving the detailed internal planning and requirement traceability that auto-mode relies on. | Yes |
